---
import { getCollection } from "astro:content";
import BaseLayout from "../../layouts/BaseLayout.astro";

const entries = (await getCollection("sessions")).sort((a, b) => a.data.id.localeCompare(b.data.id));
const base = import.meta.env.BASE_URL.replace(/\/$/, "");
---

<BaseLayout title="세션 | Fantasy Library">
  <div class="section-header">
    <h1>세션</h1>
    <span class="section-count">{entries.length}개</span>
  </div>
  <div class="list">
    {entries.map((entry) => (
      <a class="card" href={`${base}/sessions/${entry.slug}`}>
        <h3>{entry.data.name}</h3>
        <p>{entry.data.summary}</p>
        <p class="meta">{entry.data.id} · {entry.data.canon_status} · 확신도 {entry.data.confidence}</p>
      </a>
    ))}
  </div>
</BaseLayout>
